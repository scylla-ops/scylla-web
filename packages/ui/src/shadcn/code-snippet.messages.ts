import { msg } from '@lingui/core/macro';

/** `lingui extract` does not read `.svelte` — see `refacto_svelte.md` §4.5. */
export const codeSnippetMessages = {
  copy: msg`Copy`,
  copied: msg`Copied`,
};
